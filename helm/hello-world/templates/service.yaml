apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: hello-world
spec:
  type: {{.Values.service.type}}
  selector:
    app: hello-world
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP